{
  "name": "@klent/schema",
  "version": "0.0.1-alpha.4",
  "license": "Apache-2.0",
  "description": "Zod schemas and TypeScript types for the Klent API — shared between the SDK and server.",
  "keywords": [
    "klent",
    "zod",
    "schema",
    "types"
  ],
  "homepage": "https://klent.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klentlabs/klent-sdk-ts.git",
    "directory": "packages/schema"
  },
  "bugs": {
    "url": "https://github.com/klentlabs/klent-sdk-ts/issues"
  },
  "author": "Klent",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@klent/tsconfig": "workspace:*",
    "typescript": "^5.6.3"
  }
}
